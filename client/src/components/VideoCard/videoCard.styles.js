import styled from 'styled-components';

export const WrapAddButon = styled.div`
  position: absolute;
  top: 2px;
  left: 2px;
  margin-top: 0px !important;
`;

export const WrapDuration = styled.div`
  position: absolute;
  display: flex;
  align-items: center;
  justify-content: center;
  bottom: 5px;
  right: 5px;
  padding: 3px 4px;
  border-radius: 2px;
  background: rgba(0, 0, 0, 0.8);
`;
